{
  "name": "well-volume-calc",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint . --ext .js,.html",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "jsdom": "^20.0.3",
    "prettier": "^2.0.0"
  }
}
