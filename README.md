# Keino Volume Calculator

## Notes

- The smoke tests depend on the built bundle. Run `npm run build` before running `npm test` locally, or ensure your CI runs `npm run build` before tests. This prevents tests from running against an out-of-date bundle and avoids timing/behavior regressions.
